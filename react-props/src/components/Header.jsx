const Header = () => {
    return (
        <header>
            <h1>CodeCircle</h1>

            <nav>
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Account</a>
            </nav>
        </header>
    );
};

export default Header;